#include <stdio.h>

int main() {
    int rows = 3, cols = 4;
    int num = 1;
    
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            printf("%-3d", num);
            num += 2;
        }
        num -= 5;
        printf("\n");
    }
    
    return 0;
}
